================================================================================
KEY: p0Scope
DECISION 0007 — P0 scope lock (`docs/decisions/0007-kientaohub-phase-0-scope-lock.md`). ACCEPTED CONTENT: P0 launch scope is exactly the `PLAN.md` §26 P0 list (PLAN.md:2665-2691), with three adjudications forced by verified architecture. MANDATORY TABLE FORM (review fix M2): every row carries TWO columns — "P0 launch scope: in/out" (derived from §26 only) and "build owner / execution phase" — so §26 priority tiers, §27 roadmap phases, and build order are never conflated. (a) IN P0, unchanged: Auth; Categories; Product catalog; Search cơ bản; Product detail; Preview; Seller; Product upload; Moderation (the publish flow that grants a guest read on a published product row is INSIDE Moderation — without it the catalog is unusable; phase-0 plan:205); Wallet; Top-up; Payment webhook; Purchase; Entitlement; Secure download; Orders; Seller earning; Withdrawal manual approval; Review; Admin; Audit. (b) RELOCATED, not cut: "SEO cơ bản" → the Next.js storefront workstream (decision 0005:43-47,108-109 — daptin serves no sitemap/robots/cache headers); the schema keeps SEO columns/slugs (`schema_catalog.yaml`). (c) DEFERRED out of launch-blocking scope: Email (no SMTP exists, `docs/product/overview.md:74-76`; every P0 flow must be buildable without email; re-enters only by explicit owner decision — flagged in needsOwner); Logs/monitoring and Backup → the ops track (Phase 1 exit criteria, PLAN.md:2765-2771). (d) DEFERRED to P1 per §26 (PLAN.md:2695-2707): direct payment (FR-13 already marks it P1, PLAN.md:599-603 — note the "Wallet purchase là P0" line at :601 sits inside FR-13), coupon, advanced search engine (ADR 0011), collections, product-versioning UI, web push, seller analytics, automated payout, fraud detection, watermark engine, recommendations. P2 (PLAN.md:2711-2724) untouched. CUT from P0 entirely: nothing from §26; provider integration (even test mode), Next.js implementation, and deployment/TLS/CDN/backups are out of scope by the task brief and earlier decisions. ERD OF RECORD: the 0007 entity list — user_account (existing), seller_profiles, the six catalog tables, wallets/wallet_ledger (existing), and new payment_intents, payment_transactions, payment_webhook_events, orders, order_items, entitlements, download_events, seller_earnings, withdrawals, withdrawal_events, reviews; `product_tags` stays replaced by daptin's generated join table (measured, phase-0 plan:254-257). MONEY EXTENSION RULE applies to every new money table: schema entry + FinancialWriteDeniedTables entry + trigger entry in ONE commit (decision 0006:79-83). AUTHORITY: PLAN.md §26/§27; decision 0005; decision 0002; the owner's grant of full selection authority conveyed with this package (recorded in the ADR's Context, superseding decision 0006:35-36's reservation of product policy FOR THESE THREE SELECTIONS ONLY). REJECTED: locking §26 verbatim without adjudication (would claim capabilities the repo proves absent — email, SEO surface — and misstate ownership); cutting Withdrawal or Review to shrink scope (§26 lists both as launch-required, FR-32/§22 assume them); pulling Direct payment into P0 (PLAN.md:601-602 marks it P1). CLOSURE DISPOSITION (review fix M2-second): §27 also lists Wireframe and Design system as deliverables — these are re-scoped OUT of this package as owner visual-design artifacts beyond repository authority, recorded explicitly in 0007; the three §27 exit criteria (PLAN.md:2743-2747) are what gate Phase 0 closure.

================================================================================
KEY: paymentProvider
DECISION 0008 — Payment provider: **SePay** (`docs/decisions/0008-kientaohub-payment-provider.md`). ACCEPTED: SePay — the Vietnamese bank-connect aggregator offering VietQR bank-transfer checkout with real-time bank-arrival webhooks (developer.sepay.vn, sepay.vn). Rationale, each point anchored: (1) VND-native bank-transfer QR is the first rail FR-12 names ("SePay / bank transfer QR", PLAN.md:583) and the dominant low-fee method for Vietnamese digital-goods buyers; (2) webhook-on-money-arrival matches FR-12's exact chain (webhook → verify signature → idempotency → PAID → CREDIT ledger, PLAN.md:563-577) and §11.3's security list (PLAN.md:1527-1538); (3) withdrawals are manual Finance-Admin bank transfers (FR-32, PLAN.md:1019-1054), so no provider payout API is needed at P0 — SePay's lack of payout features is irrelevant; (4) VND-only matches §6.2 integer-VND (PLAN.md:271-281); (5) the FR-12 adapter mandate (PLAN.md:579) is preserved — this is a routing decision, not a lock-in; Phase 1 builds the adapter interface so VNPay/MoMo can be added. AUTHORITY: FR-12 + §11.3 + §6.2; the owner's full-authority grant conveyed with this package, recorded in the ADR (decision 0006:35-36 had reserved provider choice from the earlier grant; this new grant supersedes it for the selection — the ADR must cite both). REJECTED ALTERNATIVES: VNPay — full acquiring-gateway onboarding with contract lead time; better suited as the P1 direct-payment adapter (FR-13) than the P0 wallet top-up rail. MoMo / ZaloPay — e-wallet rails requiring verified business accounts (MoMo for Business onboarding), app-bound users, mismatched with bank-QR-first intent; P1 adapter candidates. Manual bank-transfer reconciliation (no provider) — rejected: without webhooks FR-12's automation and BR-02 idempotency degrade to human double-entry and invite double-credit. Stripe/PayPal — not VND-local, unused by Vietnamese buyers. HONESTY MARKERS (review fixes): the doc must contain, in Decision not Context: "no provider integration is built in Phase 0, not even in test mode"; "webhook authentication is the provider's signature/secret scheme — SePay's current webhook auth is a static secret token header, not a payload HMAC; the exact scheme is confirmed against provider docs at integration time" (do not write "HMAC" as if proven); "sandbox availability is asserted from public provider documentation and is unverified until registration".

================================================================================
KEY: stateMachine
DECISION 0009 — Financial state machine approved (`docs/decisions/0009-kientaohub-financial-state-machine.md`). Entity: `payment_intents` (top-up, FR-12); the §11.2 enum (PLAN.md:1513-1523) is the exact status set — no extensions. ACTORS: buyer = signed-in JWT user; provider = verified webhook; system = lazy check on read in P0 (no scheduler exists); finance = Finance-Admin action. PRIMITIVE MARKERS: ✓ = exists and measured today; ⛳ = no live primitive, owned by the Phase 1 `$payment` Go action performer — nothing in the table may be read as proven capability. TRANSITION TABLE: T1 — →CREATED | event intent_create | buyer | guard: signed-in, amount integer VND > 0 (§6.2) | effect: INSERT payment_intents, status CREATED, expires_at set; ⛳ $payment (guard reuses parseWalletAmount semantics, action_wallet.go:69-104) | failure: 400 invalid_amount, nothing written (proven class, action_wallet.go:190). T2 — CREATED→PENDING | provider_checkout | buyer/system | guard: status CREATED, not expired | effect: store provider checkout ref, status PENDING, QR displayed; ⛳ $payment | failure: 500, action transaction rolls back, stays CREATED (proven rollback class, handle_action.go:133-134). T3 — PENDING→PAID (also from EXPIRED/CANCELLED: a late verified webhook wins — money arrived) | webhook_paid | provider | guard: signature/secret valid per provider scheme (§11.3); amount == intent amount; idempotency: (provider, provider_transaction_id) unseen (BR-02) | effect: ONE action transaction: INSERT payment_transactions(PAID) + intent status PAID + $wallet credit with reference_type="payment_intent", reference_code=intent_code (✓ live primitive, action_wallet.go:261-265) | failure: bad signature → reject, nothing written (§21 Case 6); amount mismatch → intent STAYS PENDING + reconciliation flag per §21 Case 5 ("Flag reconciliation/manual review") — NOT auto-FAILED (review fix m6; the adopted interpretation is marked as new policy in the ADR); duplicate webhook → 200 no-op replay (Case 1, BR-02); DB error after webhook → 500 full rollback, provider retries (Case 7). T4 — PENDING→FAILED | verified failure webhook | provider | guard: signature valid, no PAID transaction | effect: INSERT transaction row status FAILED, no money moves; ⛳ $payment | failure: same 403/500 classes. T5 — PENDING→EXPIRED | expires_at passed | system | guard: status PENDING ∧ now>expires_at ∧ no PAID transaction | effect: status EXPIRED, no wallet write; ⛳ reconciler action | failure: none (idempotent state set); late webhook still honored via T3. T6 — CREATED/PENDING→CANCELLED | buyer cancels | buyer | guard: no PAID transaction | effect: status CANCELLED, no wallet write; ⛳ $payment | failure: cancel-after-PAID refused (409 class); late webhook still honored via T3. T7 — PAID→REFUNDED | policy refund | finance | guard: refund policy authorized (§22 restricted) — P1, NO P0 FLOW | effect: reversal LEDGER ENTRY via $wallet debit with reference_type="refund", reference_code=<original ledger reference_code> (✓ primitive exists; the finance-gating wrapper is ⛳), never UPDATE (BR-03, PLAN.md:1393-1401; trigger-enforced, financial_guard.go:53-63); intent → REFUNDED | failure: insufficient balance → 409 insufficient_funds (action_wallet.go:221-227); refund-loss policy (platform-absorb) is Phase 1 product policy. TERMINAL: EXPIRED, FAILED, CANCELLED, REFUNDED (refund of a REFUNDED intent refused). RULINGS: the wallet itself has NO state machine (its status column stays performer-managed bookkeeping; balance derives from the ledger); FR-15 order statuses (PLAN.md:654-664) and FR-32 withdrawal states (PLAN.md:1043-1052) are SEPARATE machines approved with their own phase ADRs; payment_intents carries the §11.2 status, payment_transactions carries its own per-attempt status. HONEST CAPABILITY STATEMENT (mandatory in the ADR): every §11.2 state is NOT live today because payment_intents/payment_transactions do not exist until this package's schema step; only the $wallet credit/debit legs are live and measured. Invariants that ARE live and gate every transition: no negative balance (BR-01, concurrency-proven 3×200/7×409), money+state in exactly one action transaction, no UPDATE/DELETE on wallet_ledger ever. AUTHORITY: §11.2 + §21 + FR-12 + §42.5; REJECTED: a combined mega-machine spanning payment/order/withdrawal (couples lifecycles that fail independently); wallet-balance as the state carrier (would demand a wallets UPDATE path the architecture deliberately refuses); extending the enum (breaks 1:1 with §11.2).

================================================================================
KEY: apiConventions
`docs/api-conventions.md` states these rules, each anchored to observed daptin behavior — no rule may assert a capability not present in the repo: (1) SURFACE/VERSIONING (conflict resolved decisively): the wire contract of record is daptin's actual surface — `GET/POST/PATCH/DELETE /api/<table>` (JSON:API), `POST /action/<type>/<action_name>`, `GET /asset/{table}/{id}/{column}` for blobs (`daptin/server/server.go:564`; no `/api/v1` prefix exists in the router, grep-verified). PLAN.md §18's `/api/v1/...` paths are SUPERSEDED as the internal API surface (decision 0005: daptin is the API of record) and become the Next.js storefront's own public route contract mapping onto `/api/<table>`. No `/api/v1` alias layer (rejected: a daptin patch for zero consumer benefit, decision 0001 patch cost; also rejected: reverse-proxy aliasing daptin's action/asset routes bypass). Versioning = the conventions-doc version + pinned daptin `v0.13.9-patched` behavior: additive schema/actions only; breaking JSON:API behavior changes require an ADR (satisfies §33.8, PLAN.md:3116). One-line supersession note added under PLAN.md §18. (2) AUTH: Bearer JWT from `POST /action/user_account/signin`; the token is returned inside a `client.store.set` array entry (`daptin/wiki/Authentication.md:43-69`; emission located by review in the sign-in action — verify the exact Go line at write time, reviewer found `daptin/server/actions/action_generate_jwt_token.go:83` while `auth.go:135-137` is the extractor — do not cite the wrong one). All four money actions require it (Permission 2097152); money actions are row-owner-gated (a signed-in stranger cannot debit a known wallet — measured, phase-0 plan:292-299). localStorage token storage (decision 0004) acceptable local-only, revisit pre-deployment. (3) ERROR TAXONOMY — five classes, all measured: `400` + `invalid_amount`/`invalid_currency` (`action_wallet.go:146,190`; schema validation `required,gt=0`, `schema_wallet.yaml:136-138`); `403` with three named causes — TableAccessPermissionChecker direct-write refusal (the financial denylist check at `middleware_tableaccess_permission.go:91` precedes the administrator early-return at `:96` within the same InterceptBefore method — cite exactly this, not a cross-method span), `financial_write_denied` write-method action outcomes, and the AuthenticatedExecute action-gate refusal; `404` unknown entity (local patch); `409` + `insufficient_funds` — refused, NOTHING moved (`action_wallet.go:221-227`); `5xx` — mid-action failure with FULL rollback. Documented quirk: bare `PATCH/DELETE /api/<table>` without an id serves the SPA shell with HTTP 200 and no API effect (phase-0 plan row 11) — clients always address by id. Callers branch on status+code, never 200-with-flag (0006 Alternative 3). (4) PAGINATION: JSON:API offset only (`page[number]`, `page[size]`); cursor pagination PROHIBITED (documented broken, `wiki/Filtering-and-Pagination.md:490-495`); `contains` requires caller-supplied `%`. (5) IDEMPOTENCY: webhook replay protection = BR-02 unique constraint `(provider, provider_transaction_id)` on payment_transactions — lands with this package's schema step. Money-action correlation: the wire InField is `reference_id` (mapped by the performer to the ledger column `reference_code`, because daptin reserves `reference_id` as the system bytea identity — `schema_wallet.yaml:69-76`); the conventions doc states BOTH the current wire shape and the target contract where `reference_type`+`reference_id` become required (enforced when the schema step adds the validation). Purchase idempotency is required (FLOW-U03, PLAN.md:1120-1122); the client-generated `order_code` mechanism is marked PROPOSED-UNVERIFIED and decided by the Phase 1 order ADR (review M3 fix). (6) RATE LIMITS: daptin's per-IP route limiter via `limit.rate` config (per-route JSON limits, default empty = unlimited, `X-RateLimit-*` headers; `server/middleware_ratelimit.go:20,125-127`, `server/server.go:176-185,205`) plus `limit.max_connections` default 100/IP. Phase 1 defaults (stated as defaults, not guarantees): strict on auth actions, moderate on `/api/*`, generous on the webhook route plus provider allowlist — an allowlist never replaces signature verification (§11.3:1535). (7) Every rule validated by an already-executed probe or file:line; the doc must contain the sentence that no live administrator HTTP measurement exists.

================================================================================
KEY: threatModel
`docs/threat-model.md` maps PLAN.md §32's twenty threats (PLAN.md:3084-3103) 1:1, each row: existing control (file:line or "none") / missing control / owning phase (§27 vocabulary). T1 URL guessing for original files: product_files table gate 1 + owner-only rows (`schema_catalog.yaml:222-228`), asset route checks per request / storage-key unguessability not asserted + publish-flow row grants + download gate / Phase 1 (ADR 0010). T2 Signed-URL sharing: none exists (no PresignGetObject, grep-verified; proxy ties to row permission, phase-0 plan:75-78) / short-TTL one-time token + entitlement check / Phase 1 (0010). T3 Credential stuffing: daptin signin exists / no rate limit configured (default unlimited), no lockout / Phase 1 `limit.rate` + observability. T4 Fake payment webhook: none (no integration); denylist limits blast radius today (`financial_guard.go:24-27`) / provider signature/secret verification per provider scheme (§11.3) / Phase 1 payment slice (0008/0009). T5 Webhook replay: design only / BR-02 unique constraint / lands with this package's schema step. T6 Double-spend wallet: PROVEN — FOR UPDATE + 409 refusal, measured 3×200/7×409 twice independently (`action_wallet.go:207,221-227`; phase-0 plan:470-473) / none; regression = re-run the concurrency probe on every money-table commit / standing gate. T7 Withdrawal race: same $wallet debit primitive exists / withdrawals table + reserve-balance design + race test / Phase 1 withdrawal slice (FR-32). T8 Seller malware upload: none / async scan worker (§42.12 forbids in-request heavy work), status field on product_files / Phase 1 upload slice. T9 Stored XSS: none server-side / Next.js escapes by default + sanitize policy for previews/comments + moderation / Phase 1 catalog+storefront. T10 IDOR on others' orders: owner-scoped row permission pattern proven (0002/0006; schema_wallet.yaml owner rows) / apply pattern to orders/entitlements / Phase 1 order slice. T11 Admin privilege escalation: become_an_administrator closed (non-empty administrators group); denylist precedes the administrator early-return (middleware_tableaccess_permission.go:91→:96); TestFinancialDenylistBindsAdministrator — MANDATORY literal sentence: "no live administrator HTTP measurement was possible — no obtainable credential; proof is mechanism-level only" / credential custody + live admin matrix when obtainable / ops (RUNBOOK.md:28-30). T12 Spam product: FR-28 states designed / publish/moderation gate + rate caps / Phase 1 moderation. T13 Copyright abuse: copyright_declared column (`schema_catalog.yaml:183`) / report/dispute/takedown flow / Phase 1 (P2 acceptable). T14 Catalog scraping: offset pagination is crawl-easy — ACCEPTED for P0 because SEO wants crawlers / rate limits + storefront caching / Phase 1/P1. T15 Bot download: none / download counters + heuristics (FR-17:711-714) + entitlement enforcement / Phase 1 with 0010. T16 CSRF: Bearer-token API, no cookies — classic CSRF largely inapplicable; JWT in localStorage (decision 0004) / re-evaluate if auth moves to cookies / pre-deployment 0004 review. T17 SSRF via URL import: the feature does not exist — the existing control is exactly that (do NOT cite financial_guard.go; it guards SQL table names, not SSRF — review fix) / if ever built, denylist-style review gate / cut at P0. T18 Zip bomb: none / scan stage + size caps / Phase 1 upload scan. T19 MIME spoofing: none / MIME/checksum validation in scan / Phase 1 upload scan. T20 Large-upload resource exhaustion: limit.max_connections per IP default 100 (`server.go:168-171`) / upload size caps + streaming limits / Phase 1 upload. DOC RULES: a row may claim a control only with file:line; every "missing" cell must be grep-verifiable absence; the §42 prohibitions are cited where they bind (T1←§42.1, T8←§42.12, T4/T5←§42.4/5).

================================================================================
KEY: deferredDecisions
(1) Secure-download path — RESOLVED as ADR 0010: entitlement-gated proxy download inside daptin. A Phase 1 `$download` Go action performer (`POST /action/products/download`) verifies entitlement, logs a `download_events` row, and serves bytes through daptin's existing per-request permission-checked asset streaming path (`daptin/server/server.go:564`; `asset_route_handler.go` per phase-0 plan:69-74), fronted by a short-TTL one-time token (1-10 min per FR-17, PLAN.md:705-707) so FR-17's intent-control goal is met without a shareable URL. Honest re-framing (required): FR-17's literal 'signed URL ngắn hạn' is NOT met by this mechanism; ADR 0010 states this and names the pre-approved trigger — when public deployment/CDN arrives, `PresignGetObject` becomes the sanctioned 15th daptin patch with `GET /downloads/{product}` + 302. Rejected: presign-first (a 15th patch on hot upstream storage code before any deployment exists — decision 0001); a separate signing service holding storage credentials (second write-path discipline, rejected for the same reason 0006 rejected a separate money service); static/public URLs (§42.1). Authority: decision 0005 Follow-Up:116-119, phase-0 plan:67-78. (2) Search engine — RESOLVED as ADR 0011: P0 search = daptin's existing JSON:API filtering (LIKE/contains over products.title/slug/product_code + taxonomy filters; `resource_findallpaginated.go:1585-1659` is the only capability that exists), with the accepted P0 limitation stated explicitly: Vietnamese diacritics/typo tolerance (FR-04:426-433) are NOT met — documented as a P0 acceptance gap, not dropped. Meilisearch is SELECTED as the P1 engine (consistent with §26 P1:2699 and §43 scale row PLAN.md:3435-3437); the index-sync wiring (candidate: storefront-side reindex) is a named P1 design item, not decided here. Rejected: adding tsvector support to daptin (a query-builder patch for a P1 need); deploying an external engine during P0 (no deployment story exists). One-line supersession note under PLAN.md §43's search row. (3) Ledger append-only enforcement — ALREADY DECIDED by 0006(c); CONFIRMED, not re-opened, and no new ADR: triggers boot-installed and fatal-on-failure (`financial_guard.go:53-99,116`), denylist before admin early-return, extension rule (0006:79-83). The confirmation paragraph lives in ADR 0007 + an amended decisions-README row; the one missing enforcement primitive (BR-02 unique index) lands with this package's schema step. Residuals stand unchanged and are restated wherever relevant: no DB barrier on wallet_ledger INSERT or wallets UPDATE (0006 amendment), `$transaction` refusal lexical.

================================================================================
KEY: implementationSteps
["Step 1 — `docs/decisions/0008-kientaohub-payment-provider.md` (new, per docs/templates/decision.md). Content: SePay selection with the four anchored rationale points (FR-12:563-587 with SePay named at :583; §11.3:1527-1538 webhook security; §6.2:271-281 VND integer; FR-32:1019-1054 manual withdrawal removing the payout-API requirement); the authority paragraph recording this owner grant and superseding 0006:35-36's reservation for the selection only; rejected alternatives (VNPay, MoMo, ZaloPay, manual reconciliation, Stripe/PayPal) each with its rejection reason; adapter rule; the sentences 'no provider integration is built in Phase 0, not even in test mode' and 'webhook authentication is the provider's signature/secret scheme (SePay uses a static secret token header today), confirmed against provider docs at integration time' and 'sandbox availability: unverified until registration'. No dependency.", "Step 2 — `docs/decisions/0009-kientaohub-financial-state-machine.md` (new). Content: the exact §11.2 enum (PLAN.md:1513-1523); the seven-row transition table from the stateMachine field verbatim, with ✓/⛳ primitive markers per row; the wallet-has-no-state-machine ruling; the 'separate machines' ruling for FR-15 orders and FR-32 withdrawals; the rejected alternatives; the explicit 0006 Follow-Up reference for the payment-table declaration (this package, one commit); the payment_intents-vs-payment_transactions status-carrier clarification. Depends on: step 1 (provider named in T3).", "Step 3 — `docs/decisions/0007-kientaohub-phase-0-scope-lock.md` (new). Content: the scope table with MANDATORY two-column disambiguation per row — 'P0 launch scope: in/out' (from §26:2665-2691 only) and 'build owner / execution phase' (daptin slice done / Phase 1 slice / storefront track / ops track) — so §26 priority tiers, §27 roadmap phases, and build order are never conflated (review M2 fix); the relocated rows (SEO→storefront; Email deferred; Logs/monitoring+Backup→ops track) each with authority; deferred-to-P1 and P2 lists; the ERD-of-record entity list (user_account, seller_profiles, the six catalog tables, wallets/wallet_ledger, payment_intents, payment_transactions, payment_webhook_events, orders, order_items, entitlements, download_events, seller_earnings, withdrawals, withdrawal_events, reviews; product_tags replaced by daptin's join table per phase-0 plan:254-257); the ledger-enforcement confirmation paragraph; the rejected alternative ('lock §26 verbatim without adjudication'). Depends on: steps 1-2.", 'Step 4 — `docs/decisions/0010-kientaohub-secure-download-path.md` and `docs/decisions/0011-kientaohub-search-engine.md` (new). Content: the resolutions in the deferredDecisions field, each rejected alternative citing its evidence (no PresignGetObject — grep-absent, phase-0 plan:75-78; no FTS — resource_findallpaginated.go:1585-1659; §43:3435-3437; FR-04:426-433). Depends on: step 3.', 'Step 5 — `docs/api-conventions.md` (new). Content: the seven rule blocks in the apiConventions field verbatim, including the reference_id→reference_code wire mapping with both current and target contract state, the :91→:96 same-method citation, the 200-SPA-shell quirk, the PROPOSED-UNVERIFIED order_code marker, and the mandatory no-live-admin-measurement sentence. Depends on: steps 2 and 4.', "Step 6 — `docs/threat-model.md` (new). Content: all twenty §32 threats (PLAN.md:3084-3103) in order, table format (threat / existing control with file:line or 'none' / missing control / owning phase using §27 vocabulary), with the review fixes applied: threat 17's existing control is 'no URL-import surface exists' (do NOT cite financial_guard.go — it guards SQL table names, not SSRF); threat 4's guard wording is 'provider signature/secret verification per provider scheme'; threat 11 must contain the literal sentence 'no live administrator HTTP measurement was possible — no obtainable credential; proof is mechanism-level (denylist ordering, TestFinancialDenylistBindsAdministrator, role-independent triggers)'; threat 6 states the measured 3×200/7×409 concurrency proof and the re-run gate on every money-table commit. Depends on: steps 3-5.", 'Step 7 — The payment-table declaration, ONE commit per the 0006 extension rule (0006:79-83, 198-209): (a) new `schema/schema_payment.yaml` declaring `payment_intents`, `payment_transactions`, `payment_webhook_events` per PLAN.md §11.1:1466-1494 columns (payment_transactions gains provider_transaction_id + the BR-02 unique index on (provider, provider_transaction_id)); every table carries the decision-0002 authorization block (explicit AccessGroups + non-zero Permission, owner-scoped rows, no world write bits, money gates deny Create/Update/Delete); no quoted-string DefaultValue (schema/README.md rule); (b) add the three table names to FinancialWriteDeniedTables and the trigger installer in `daptin/server/resource/financial_guard.go` (minimal edit to an already-patched file — patch cost recorded per decision 0001); (c) `docker compose restart daptin` (schema content change), verify the boot gate. Depends on: steps 1-6 (documents first, so the commit message can cite them).', "Step 8 — `docs/decisions/README.md` (update): index rows 0007-0011 in the existing table format; fix the stale patch count in the 0001 row ('thirteen patches' → 'git log --oneline origin/master..HEAD', currently 10); append the 0006 row's confirmation note (BR-02 index landed with schema_payment.yaml). Depends on: steps 1-7.", "Step 9 — `PLAN.md` annotations (two one-line revertible notes): under §18, '/api/v1/... paths superseded as the internal API surface by decisions 0005 + docs/api-conventions.md'; under §43 search, 'MVP search is daptin JSON:API filtering per decision 0011; PostgreSQL FTS row deferred to P1'. Depends on: steps 5, 4.", 'Step 10 — `docs/plans/active/kientaohub-phase-0.md` (update): tick or explicitly re-own EVERY open Progress row — :207 API conventions (done, step 5), :208 threat model (done, step 6), :209 the three locks (done, steps 1-3), :210 deferred decisions (0010/0011 done; ledger enforcement confirmed in 0007), :204 remaining §17 entities (closed as: the 0007 entity list is the ERD of record; payment tables physically declared in step 7; the rest are Phase 1 schema slices), :205 publish flow (re-owned to the Phase 1 moderation slice with the gap restated — no product row is guest-readable until it lands); add a Decisions entry per ADR; add a Validation section with the probe results from step 7 and the standard honesty markers (no live admin HTTP measurement; FTP failure at daptin/server_test.go:1491 is pre-existing environmental drift, not caused by this package). Depends on: steps 1-9.', 'Step 11 — `docs/product/overview.md` (update): one short paragraph — P0 scope locked, financial state machine approved, SePay selected (paper only), payment tables declared; no behavior claims change. Depends on: step 10.', 'Closure rule — the active plan moves to docs/plans/completed/ only when: the three §27 exit criteria hold (scope locked = 0007, state machine approved = 0009, provider chosen = 0008), the five ADRs + two documents exist and pass their validation, and every progress row is ticked or re-owned. Wireframe and design system (§27 deliverables) are explicitly re-scoped out of this package as owner visual-design artifacts beyond repository authority, recorded in 0007 — they do not block the exit criteria (PLAN.md:2743-2747 lists exactly three).']

================================================================================
KEY: validation
["ADR 0007 (scope): every in-P0 row traces to PLAN.md:2665-2691 by quote; every deferral/relocation cites its authority line; a grep of the ADR finds no uncited capability claim; the entity list cross-checks against schema/*.yaml and the phase-0 plan's catalog/wallet proofs; after step 7, `docker compose restart daptin` boot log contains `Found files to load: [... schema_payment.yaml]` with zero ERRO/WARN in the current-boot segment (slice from the last 'Found files to load' — schema/README.md pitfall) and the boot line 'financial guards installed:' now names the new triggers.", 'ADR 0008 (provider): contains FR-12 (PLAN.md:563-587), §11.3 (:1527-1538), §6.2 (:271-281) citations; contains verbatim the three honesty sentences (no Phase 0 integration; provider signature/secret scheme not assumed to be HMAC; sandbox unverified); provider facts cite developer.sepay.vn.', 'ADR 0009 (state machine): every ✓ row cross-checked against the cited action_wallet.go lines (editor opens them); every ⛳ row grep-verified absent from the repo (grep for the operation/performer name); the enum matches PLAN.md:1513-1523 character-for-character; the ADR contains the honest capability statement and the 0006:198-209 reference for the table declaration. Doc-lint: no transition claims a primitive whose name does not grep in the repo.', "ADR 0010: cites server.go:564 asset route and the upload-only presign finding; negative proof = grep for PresignGetObject (absent); contains the explicit re-framing sentence that FR-17's literal signed-URL wording is not met by the P0 mechanism and names the presign trigger condition. ADR 0011: negative proof = grep for tsvector in daptin/server/resource (absent; only LIKE/SOUNDEX at resource_findallpaginated.go:1585-1659); cites §43:3435-3437 and FR-04:426-433 as the accepted P0 gap.", 'api-conventions.md: every rule validated by an already-executed probe (phase-0 plan Validation rows 4-17) or file:line; re-run the existing probes unchanged: debit-above-balance → 409 insufficient_funds writing nothing; amount 0 → 400 invalid_amount; direct POST/PATCH/DELETE /api/wallets and /api/wallet_ledger signed-in → 403; anonymous wallet_create → 403; forced-failure action → 500 with balance and ledger count unchanged; contains the no-live-admin-measurement sentence and the reference_id→reference_code wire mapping; the 200-SPA-shell quirk cites phase-0 plan row 11.', "threat-model.md: all twenty §32 threats present in order (PLAN.md:3084-3103); every 'existing control' cell carries file:line or the literal 'none'; every 'missing' cell is grep-verifiable; threat 11 contains the mandatory literal sentence; threat 17 cites only 'no URL-import surface exists'.", "Schema step (0006 extension rule): after `docker compose restart daptin` — boot gate line 'financial guards installed:' lists the new payment-table triggers alongside wallet_ledger_append_only and wallets_no_hard_delete; direct `POST /api/payment_intents` and `POST /api/payment_transactions` signed-in non-admin → 403 (denylist); duplicate webhook simulation against the unique index → constraint violation, single credit; `go test ./server/resource/ -run TestFinancial` green; `go build ./...` ok.", 'Regression after every step: `GET /ping` → 200; boot gate line present; the concurrency probe (balance 100 + 10×30 debits → exactly 3×200/7×409, final 10, ledger sum 90, zero chain violations) re-run after step 7 since it touches the money schema; the known FTP failure at daptin/server_test.go:1491 is pre-registered as environmental drift so it is never misread as a regression from this package.', 'Closure check: docs/plans/active/kientaohub-phase-0.md has every progress row ticked or re-owned with the re-ownership stated in the file; PLAN.md annotations present under §18 and §43; decisions/README.md rows 0007-0011 present and the stale patch-count line corrected; only then does the plan move to docs/plans/completed/.']

================================================================================
KEY: needsOwner
["1. New-grant recording. The authority for the three locks is the owner's grant conveyed with this task ('full authority to select the technology and proceed without further questions'), which supersedes decision 0006:35-36's reservation of provider/pricing policy FOR THE SELECTIONS RECORDED HERE. Why it cannot be decided from the repository: authority is an owner fact, not a repo fact. Default: each ADR records the grant and its date in Context; if the owner ever revokes it, ADRs 0007-0009 revert to Proposed.", '2. Administrator credential for live admin HTTP measurement. Why: the credential exists outside the repository (RUNBOOK.md:28-30) and no amount of repo inspection obtains it. Default: mechanism-level proof (denylist-before-admin-return + TestFinancialDenylistBindsAdministrator + role-independent triggers) accepted as sufficient; every relevant document carries the honesty sentence; the owner runs the 6-row admin matrix when a credential is available — the single largest standing evidence gap.', "3. SePay business registration / account type (personal vs business) and sandbox verification. Why: depends on the owner's legal entity and a provider contract, unverifiable from the repository (sandbox availability marked unverified). Default: SePay stands as the paper selection; all provider work sandbox-only until registration; if a registered business proves mandatory, the recorded fallback order is VNPay → MoMo Business.", '4. Commission rate value and earnings hold period (§6.3:285-311, FR-31). Why: owner pricing policy, explicitly outside any technical grant. Default: no number in any Phase 0 document; order_items carries the full snapshot column set (sale_price, platform_fee, seller_amount, tax, applied_policy_version) from day one so a rate lands later as configuration without schema change.', '5. Top-up amount bounds (min/max) and FR-17 TTL final values. Why: product risk policy with no repository authority. Default: only the proven amount>0 integer-VND guard; bounds become action validations when the top-up action is written; download token TTL 1-10 min per FR-17:705-707 until an owner number exists.', "6. Email re-entering launch-blocking P0. Why: 'Email' sits in §26's P0 list and this package adjudicates it out (no SMTP exists, overview.md:74-76) — a product-priority call. Default: deferred; every P0 flow must be buildable without email; verification/password-reset hardening lands in P1. The owner confirms or overrules when reviewing ADR 0007."]

